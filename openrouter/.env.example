# Copy this file to .env and paste your key. Get one at https://openrouter.ai/keys
# Note: .env must be saved without a UTF-8 BOM, or Node's --env-file silently
# fails to see the first variable.
OPENROUTER_API_KEY=
WEATHER_SOURCE=mcp
